using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class bulletHoleTargetBehavior : MonoBehaviour
{
    public bool onTarget = false;
    // Update is called once per frame
    void Update()
    {
        if (onTarget) { 
            
        }
    }
}
